import styled from "styled-components/native";
import {RFPercentage} from "react-native-responsive-fontsize";

export const ListaBotoes = styled.FlatList`
    background-color:${({theme})=>theme.PRIMARIAS.100}
`;